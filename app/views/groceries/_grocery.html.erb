<div id="<%= dom_id grocery %>">
  <div class="columns is-vcentered">
    <div class="column">
      <h2 class="title is-4"><%= grocery.name %></h2>
    </div>
    <div class="column is-narrow">
      <%= button_to "Delete",{:controller => :groceries, :action => 'destroy', :id => grocery.id}, confirm: 'Are you sure?', :method => :delete, class: "button is-danger" %>
    </div>
  </div>
</div>
